# This is a Declarative Automation Bundle definition for spotify_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dab
  uuid: a9167bde-7986-4732-8350-cf23d5a94284



# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405605622150609.9.azuredatabricks.net
      root_path: /Workspace/Users/ilyass.rbaai@emsi-edu.ma/.bundle/${bundle.name}/${bundle.target}
    presets:
        source_linked_deployment : false
    variables:
      catalog: databricksazurespotify
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://adb-7405605622150609.9.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/ilyass.rbaai@emsi-edu.ma to make sure we only have a single copy.
      root_path: /Workspace/PROD/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: databricksazurespotify
      schema: prod
    permissions:
      - user_name: ilyass.rbaai@emsi-edu.ma
        level: CAN_MANAGE
